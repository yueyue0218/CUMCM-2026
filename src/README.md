# 正式代码

- `q1/` 至 `q4/`：对应各分问题的正式代码和运行入口。
- `common/`：公共的数据读取、清洗、评价指标、绘图和工具函数。
- 每个分问题建议以 `main.py` 为入口，所有路径相对于仓库根目录。
- 输入只读取 `problems/`、`data/raw/` 或 `data/processed/`。
- 输出统一写入 `results/`，不得覆盖原始数据。
- Notebook 中确认有效的算法应迁移到这里。
