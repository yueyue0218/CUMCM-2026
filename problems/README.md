# 赛题与官方附件

- `A/`、`B/`、`C/` 保存对应题目的原始题面和官方附件。
- 当前已选择 B 题，正式工作以 `B/` 为准。
- 原始题面和附件只读，不要覆盖、重命名内部内容或保存程序输出到这里。
- 如果需要从 DOCX、PDF 或 Excel 中提取数据，提取结果写到 `data/raw/`，清洗结果写到 `data/processed/`。
- 大于 GitHub 单文件限制的附件不要强行提交；先在 README 中记录来源和校验值。
