# 正式测试支撑材料

模拟器导出的官方加密日志保持原文件名，按问题放入 `q3_official_logs/` 或 `q4_official_logs/`。不要编辑、解密、重命名或用自建 JSONL 日志替代官方文件。

每次正式测试的案例编码、结果指标、Git commit 和版本标签记录在对应 `runs/q3/formal/` 或 `runs/q4/formal/` 目录，并同步至论文表格。
