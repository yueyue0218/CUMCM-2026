# 符号与单位

| 共同量 | 现稿符号与对应关系 | 单位 | 固定口径 | 来源 |
|---|---|---|---|---|
| 目标圆域 | \(\Omega\)；Q1的圆域半径 \(R=1800\) | 坐标、半径为米 | 不能与Q2的未知接收半径 \(R\) 混读；跨问汇总时圆域半径明确写为1800米 | Q1式（3）、Q2式（3）、Q3式（1） |
| 源的有效接收半径 | Q2的 \(R\) 对应Q3逐频道 \(R_j\)；积分变量 \(r\) | 米 | 1000—1500，同一源在任务内固定；1000仅为保守保证接收半径 | Q2式（4）、Q3第1节 |
| 近距离阈值、清除半径 | 5、20；Q1的 \(\rho=20\) | 米 | 均包含距离边界；不是定位区域直径阈值 | 各问响应及清除契约 |
| 测角误差半角 | \(\delta\) | 输入为度、公式为弧度 | 主模型 \(1^\circ=\pi/180\)，不与条件性1.005度口径叠加 | 各问假设 |
| 指定点全域距离、最小包围圆半径 | Q1的 \(R_Q(c)\)、\(r_*\)，Q3的 \(R_K(c)\)、\(r_*(K)\)；总览早期 \(R_{\mathrm{MEC}}\) 为后者的别名 | 米 | 集合必须相同才能比较；已知半径小不等于任意点可清除 | Q1第3—4节、Q3式（29）—（35） |
| 虚拟时间及增量 | \(T,T_t,\Delta T_t\)；单步代价 \(c(X_t,a_t,z_t)\) | 秒 | 移动速度5米/秒；检测、切频、清除分项计入 | Q3式（5）—（7） |
| 奖励的固定时间尺度 | \(T_s=100\) | 秒 | 所有阶段一致，\(r_t=-\Delta T_t/T_s\) 为无量纲奖励 | Q3式（67d）、训练式（T5） |

同一概念在代码、数据字典、图片、表格和论文中必须使用一致的含义与单位。

## q1 重组稿符号索引（2026-09-11，待队友复核）

来源：[q1 基础模型](../paper/drafts/B-q1：基础模型建立.md)。

| 符号 | 含义 | 单位 | 范围或使用限制 | 正文位置 |
|---|---|---|---|---|
| S_i、G | 检测点、待定位干扰源 | 米 | 同一频道分别维护约束 | 第1.1节 |
| theta_i、alpha_i、delta_i | 示向度、真方位、误差半角 | 输入度、内部弧度 | 标准1度；1.005度为条件性防舍入策略 | 式（1） |
| W_i、P | 前向楔形、纯测向交会区域 | 坐标为米 | P可能为空、无界或退化 | 式（2） |
| Omega、R、K | 目标圆域、半径、融合圆域的区域 | 米 | R=1800；K后续可继续融合有效接收圆盘 | 式（3） |
| Q、K_hat | 通用待覆盖紧集合、经认证数值外包 | 坐标为米 | 第3节固定使用P，第4节推广为Q | 第4节、式（9） |
| A（矩阵）、b | 半平面系统系数 | 随归一化约定 | 不与最远点对符号混用 | 式（2） |
| V_j、A_*、B_*、M | 顶点、最远点对及其中点 | 米 | A_*和B_*须为真正最远点对 | 式（5）—（6） |
| D、r_*、C_* | 区域直径、最小包围圆半径及圆心 | 米 | D一般不等于2r_* | 式（5）—（8） |
| R_Q(Z)、C_out | 相对指定中心的全域最大距离、实际发送坐标 | 米 | 输出舍入后仍须检查全域 | 式（6）、式（9） |
| rho | 清除半径 | 米 | rho=20；Jung阈值为rho√3，直径必要界为2rho | 第3.2节 |
| D_L、D_U、L_r、U_r | 直径和最优半径的可靠下/上界 | 米 | 存在性上界不一定关联某个已知圆心 | 第3.2节、式（12） |
| F、r_F,L、C_F | 真实边界子集、有限点最优半径下界、子问题圆心 | 米或点集 | 可行候选圆半径不能直接作最优值下界 | 第4.4节 |
| h_K、u(phi) | 支持函数、单位方向向量 | 米、无量纲 | 须检查圆弧内极值 | 式（11） |
| mathcal A、C_g | 面积、几何质心 | 平方米、米 | 不等于概率最高位置或最优清除点 | 式（14）—（16） |
| epsilon_D、epsilon_r | 目标区间宽度 | 米 | 初值0.001米为待验证参数，临界处继续细化 | 第4.4节 |

术语统一用“最小包围圆”；它包含整个区域，不要求区域每个顶点都在圆周上。三点外接圆是Welzl强制边界子问题中的局部概念。

二次审查补充符号（沿用式（1）—（18）编号，新增接口公式按章节定位）：

| 符号 | 含义 | 单位与限制 | 正文位置 |
|---|---|---|---|
| a、Delta、b=a+Delta | 圆弧起角、逆时针扫角、连续终角 | 弧度；0≤Delta≤2π，整圆另作标记；不取最短方向差 | 第1.1节 |
| mathcal F、mathcal E | 全部有效信息下真实可行位置集合、排除约束表 | F⊆K⊆K_hat；不与Welzl有限点子集F混淆 | 第4.6节 |
| K^+(S,z)、z | 候选检测点与假定观测下的预测凸外包、观测状态/读数 | z包含direction、near及no_signal；不是已发生观测 | 第4.5节 |
| A_pred、A_bear、Delta A | 融合状态的预测面积、纯测向预测面积、面积缩减 | 平方米；相对缩减率仅在当前面积非零时定义 | 第4.5节 |
| Z(S)、S_cur | 可能观测集合、当前机器狗位置 | 期望指标需概率模型；移动耗时按5米/秒 | 第4.5节 |

第2节补充：theta_raw、theta_deg分别为原始/归一示向度（度），theta为计算用弧度；L(x)、U(x)为纵坐标下/上界，I_x、I_y为坐标投影区间；f为半平面残差；d_ij²为顶点距离平方（平方米），T(i,j)为三角形面积的两倍（平方米），J_i为当前边对应的最远支撑顶点索引集合。卡壳使用连续索引，顶点按周期m读取。

第三轮审查补充：G_h为允许输出网格，h为网格间距（米）；e为另有控制误差界时的偏差上界（米），C_(20−e)(Q)为计入该误差后的全域覆盖中心集合。F_t、C_t、r_t、q_t分别为增补点集、有限点最小圆心/半径及全域最远点，N为新增点数。tau为含移动/换频道/检测的耗时（秒）；f_worst、f_exp为面积缩减率（平方米/秒），H为有效历史，p_z为下游提供的观测概率。F_rem为K融合排除表后的源位置集合，可非闭，不是允许动作中心集合。

第四轮审查补充：C_0为连续中心逐坐标取最近网格点后的坐标；N_h(C_0)为包含中心和8邻点的局部集合，V_h(C_0)为经可靠上界校验的可行子集。Gamma为单条圆弧，X_far为沿C−Z方向的径向最远候选点，V_Gamma(Z)包含弧端点及适用的径向点；其位置单位为米，C=Z时不使用径向除法。

## Q3及训练配套符号索引（2026-09-12，待整稿复核）

来源：[Q3主稿](../paper/drafts/B-q3：全向干扰源搜索定位与清除策略.md)、[训练方案](../paper/drafts/B-q3：PPO训练方案.md)。本表明确跨问映射与局部作用域，不要求修改已经完成的Q1接口或程序字段。合并论文时应保留下标、字体及集合参数，不能因字母相同便认定为同一量。

| 符号 | 含义与单位 | 范围或区分 | 位置 |
|---|---|---|---|
| \(G_j,p_t,q,s_k\) | 源位置、当前机器狗位置、候选动作位置、骨干检测点；米 | \(G_j\in\Omega\)，机器狗动作位置可在圆域外；\(k=0,\ldots,6\) | 主稿第1—3节 |
| \(r_g=\|g\|\) | 七点覆盖证明中的源位置径向距离；米 | 外环取1000—1800；原稿局部ρ已改名，避免与Q1清除半径 \(\rho=20\) 混淆 | 主稿第3.1节、式（11） |
| \(\rho_{j,t}(q)\) | 给定历史下在候选点接收到频道j的后验概率；无量纲 | 取值0—1；不是几何半径，也不是PPO新旧策略概率比 \(\varrho_t\) | 主稿第6.1节、式（38）—（39） |
| \(e_j,N,\mathcal J,b_t\) | 源存在性、源总数、频道全集、当前测向频道；无量纲 | \(e_j\in\{0,1\}\)，\(10\le N\le16\)，20个频道；清除不改变 \(b_t\) | 式（1）、（2）、（6） |
| \(H_{j,t},X_t,D_{j,t},\sigma_{j,t}^{Q1}\) | 逐频道有效历史、总状态、Q1评估结果及其状态；复合对象 | \(D_{j,t}\) 是接口结果，不是几何直径 \(D(K)\) | 式（I1）、（8） |
| \(\mathcal P_t,\mathcal C_t,\mathcal E_t,\mathcal A_t,\mathcal U_t\) | 曾确认有源、已清除、原本无源、活动、未知频道集合 | \(\mathcal C_t\subseteq\mathcal P_t\)，\(\mathcal A_t=\mathcal P_t\setminus\mathcal C_t\)；\(\mathcal P_t\) 不是Q1几何集合 \(P\) | 式（9）、（9a） |
| \(\mathcal F_{j,t},\mathcal S_{j,t},K_{j,t}\) | 位置—半径联合可行集、位置投影、认证凸外包 | 前者包含位置和半径；后两者为位置集合，\(\mathcal S_{j,t}\subseteq K_{j,t}\)，不能以外包面积代替精确面积 | 式（20）—（25） |
| \(\pi_{j,t},\Pi_t,\mu_\vartheta,\beta_t\) | 逐频道后验、全场景联合信念、PPO策略、实际行为分布 | 信念与动作分布不同；全场景联合信念保留源数相关性 | 第8.6节、式（67h） |
| \(B(q,r),B\) | 闭圆盘、切频次数 | 圆盘带位置和半径两个参数，\(B\) 单独出现于时间账本时为整数次数 | 式（7）、（13） |
| \(\mathcal N_{j,t},U_{j,t},\nu_{jk,t}\) | 有效无信号位置集、未覆盖源位置区域、骨干点实测标志 | \(\nu\in\{\bot,0,1\}\)：缺失、有效无信号、有效有信号；\(U_{j,t}\) 不是频道集合 \(\mathcal U_t\) | 式（15）—（16a） |
| \(c_{j,t}^{Q1},\mathcal C_j^{\mathrm{safe}}\) | Q1认证清除点、全部认证清除中心集合；坐标为米 | 后者是允许动作位置，不是源位置集合，也不是已清除频道集合 \(\mathcal C_t\) | 式（I1）、（33） |
| \(U,\kappa,\Lambda\) | 逼近距离上界、收缩因子、逼近路程上界 | \(U,\Lambda\) 为米；\(U\) 绑定产生方向的参考点 \(s\)，不一定对应当前 \(p_t\) 或Q1圆心；\(\kappa=1/(2\cos\delta)\) 无量纲 | 主稿第7节 |
| \(L,M,B,C,F\) | 移动路程、检测/切频/成功清除/失败清除次数 | \(L\) 为米，其余为整数；\(M\) 不是Q1最远点对中点，\(F\) 不是联合可行集 | 式（7）、（71）—（73） |
| \(\overline V_0,Q_h,J_h,V_\omega\) | 认证后备时间上界、动作前瞻成本、剩余规划成本、价值网络输出 | 前三者为秒，\(V_\omega\) 为回报单位；\(-T_sV_\omega\) 才还原为秒，且只是估价 | 第8.1、8.6、8.8节 |
| \(\vartheta,\omega,h_t,F_t,m_t\) | 策略/价值参数、循环状态、候选输入矩阵、动作掩码 | \(\vartheta\) 不是示向度 \(\theta\)；训练的 \(F_t\) 不是Q1增补点集或Q3失败次数；\(m_t\) 是候选掩码，控制模式另带上标 | 训练第3.3、6.4—6.5节 |
| \(\varrho_t,\epsilon,\lambda\) | PPO新旧概率比、概率比截断系数、GAE系数；无量纲 | \(\varrho_t\) 不是Q1清除半径 \(\rho\)，\(\epsilon\) 不是几何容差；起点0.2与0.95为待验证参数 | 式（67e）—（67g） |
| \(\widehat A_t,\widetilde A_t,\widehat G_t,G_{t_f}^{\mathrm{fb}}\) | 原始优势、标准化优势、价值目标、完整后备实得回报 | 均为回报/归一化量，不是秒或面积；价值目标不由标准化或裁剪优势重建 | 训练第5.4—5.6节 |
| \(\mathcal I_{\mathrm{choice}},\mathcal I_V\) | 本批自由选择步索引、有效价值样本索引 | 强制后备不进入前者，可进入后者；两者均排除规划器轨迹和填充项 | 式（67g）、训练式（T15） |
| \(D_{\mathrm{geo}},h_{\min},N_{\mathrm{geo}},\tau_{\mathrm{geo}}\) | 数值细分最大深度、最小空间尺寸、单元处理上限和现实计算时长上限 | 尺寸为米、时长为秒，其余为整数；预算结束不等于定位收敛 | 式（23c） |
| \(v_i,\Delta T_i^{\mathrm{receipt}}\) | 当前动作可信回执的累计虚拟时刻、相对上次已确认时刻的增量；秒 | 回执累计值直接赋予 \(T\)，每个请求仅计一次，不与单步预测重复累加 | 式（69a） |

总览早期 \(P_{\mathrm{loc}}\) 对应仅用测向的Q1集合 \(P\)，\(P_{\mathrm{feasible}}=P\cap\Omega\) 只融合圆域；Q3的 \(\mathcal S_{j,t}\) 还涉及实际已支持的接收、近距离及排除约束，不能无条件与前者等同。总览Q2的 \(C_{\mathrm{safe}}\) 是保证接收的检测点集合，Q3的 \(\mathcal C_j^{\mathrm{safe}}\) 是保证清除的动作中心集合，半径分别为1000米与20米，作用不同。上述别名只作阅读映射，未支持的约束仍须在Q1适配说明中明确。


## q4 严格理论参考符号索引（2026-09-12，历史）

来源：[第四问严格理论参考](../paper/drafts/B-q4：严格覆盖理论与七点基准.md)。以下符号在理论参考中使用；角度内部为弧度，距离为米，时间为秒。沿用Q1集合与清除半径含义，不改变前三问已有符号。

| 符号 | 含义 | 单位或约束 |
|---|---|---|
| \(E_c,G_c,R_c,M_c,\beta_c\) | 频道存在性、源位置、固定接收半径、类型及朝向 | 存在性0/1；位置/半径米；朝向弧度，仅定向分支适用 |
| \(N,N_{\mathrm O},N_{\mathrm D}\) | 总源数、全向源数、定向源数 | \(10\le N\le16\)，两类均至少1个 |
| \(u(\beta),A,V\) | 朝向单位向量、方向可见函数、信号可接收函数 | 接收要求 \(u(\beta)^\mathsf T(s-g)\ge0\) 且距离不超接收半径 |
| \(H_t,\mathcal H_t,\mathcal F_t,K_t,\widehat K_t\) | 有效历史、联合可行集、位置投影、闭凸包、认证外包 | 不将凸包面积当作精确面积；外包用于全域清除 |
| \(L_t(g),\mathcal B_t(g)\) | 正观测决定的半径下界、允许朝向集 | 米、角度集合；无信号的严格边界保留 |
| \(f_s,b_s,\underline f,\overline f,\underline b,\overline b\) | 距离/方向残差及其可靠区间端点 | 米；距离超限为\(f_s>0\)，背向为\(b_s<0\)；端点外向舍入 |
| \(\mathscr B,\widehat{\mathcal H}_t\) | 闭状态盒、保留盒的有限并形成的联合数值外包 | 包含精确可行集；UNKNOWN盒保留，预算耗尽不等于空集 |
| \(\underline R,\overline R\) | 指定输出清除点到认证位置外包的最远距离下/上界 | 米；仅\(\overline R\le20\)授权该点，区间跨阈值为未确定 |
| \(\Delta_{\max},\varepsilon,a\) | 最大方向空隙、未知位置圆盘半径、三角形外接圆半径 | 弧度、米、米；\(\varepsilon\le a/2,\ a+\varepsilon\le1000\) |
| \(\nu_{c,i},\mathcal S_7\) | 每频道七点有效回执、七点集合 | 缺失/无信号/有信号；缺失不等于无信号 |
| \(\nu_{c,q},\mathcal I_t\) | 任意位置检测任务的回执三态、已纳入有效历史的请求标识集合 | 三态为\(\bot,0,1\)；有效回执去重，未决动作不产生物理约束 |
| \(\mathcal U_{c,t},\mathcal D(q)\) | 未发现定向源剩余位置—朝向状态、检测点保证可见状态 | 认证为空才能排除 |
| \(B_{\mathrm{ad}},d,N_{\mathrm{search}}\) | 每频道累计自适应存在性补测上限、七点已发现频道数、搜索检测数 | 次、个、次；重规划不重置预算，\(d\ge1\)，搜索不含已发现源主动定位测量 |
| \(N_{\mathrm{measure}}\) | 存在性搜索与主动定位的测量总数 | 次；上界为\(140+(20-d)(B_{\mathrm{ad}}+64)+16L_{\max}\)，清除、移动及切频另计 |
| \(p_D,\pi_t,\rho_t,\Psi_D,\Psi_r\) | 类型先验、联合后验、接收概率、期望直径和覆盖半径 | 概率用于排序，不替代硬界认证 |
| \(T_{\mathrm{step}},L_{\max},k,\varepsilon_{\mathrm{prog}}\) | 单步虚拟预算、主动步数上限、进展窗口、停滞阈值 | 秒、步、步、无量纲；有限预算触发后备 |
| \(r_{\mathrm{cell}},T_{\mathrm{base}}\) | 光学单元覆盖半径、指定无自适应基准时间 | 小于19.1米、小于38856秒；非任意轨迹或模拟器成绩 |
| \(A_t,A_{\max},J_t,J_{\max}\) | 已发出新检测/清除动作数及上限、所有网络请求数及上限 | 次；新动作按标识累计，含未决或被拒绝动作，重试只增加请求数；管理请求计入J_t |
| \(C_t,C_{\max},\overline V_t,V_{\max}\) | 累计本地规划时间及上限、累计虚拟时间可靠上界及上限 | 秒；虚拟上限不超过360000秒，不用拒绝响应的0覆盖可信累计值 |
| \(t_{\mathrm{now}},t_{\mathrm{ddl}},\tau_{\mathrm{reserve}},\overline\tau(a)\) | 本地单调时刻、现实截止、归档退出余量、动作虚拟耗时上界 | 秒；现实截止依据本局有效进入回执，虚拟与现实时钟分开 |
| \(\mathsf{Done}_t,\mathrm{status}_t\) | 完成证据谓词、任务运行状态 | COMPLETE/INCOMPLETE/RUNNING；预算耗尽不构成无源或清除证据 |
| \(\ell_t\) | 当前请求事务状态 | READY/LOCKED/HALTED；不同于任务完成状态，超时保持锁定，不生成其他新动作 |
| \(\mathcal Q_{c,t},\mathcal Q_t,\sigma\) | 经认证的逐频道方格子任务、合并任务点集及访问排列 | 必须覆盖剩余位置—朝向状态；开放路径只对固定点集优化 |
| \(\mathcal Q_t^{\mathrm{adm}},\mathcal C_{\mathrm{safe},t},W_t(q),q_t^{\mathrm{mm}}\) | 合法有限候选点、保证接收候选点、最坏后验包围圆半径及极小极大候选点 | 半径为米；保证接收不等于保证20米清除，最坏半径可不收缩 |
| \(\overline A,J_{\mathrm{issued}}\) | 正常执行总检测/清除动作界、实际发出请求总数 | 次；参考配置的动作界3084/3364，请求界以每原请求最多3次尝试为条件 |
| \(\widetilde R,e_R,f_{\mathrm{clr}}\) | 指定输出点最远距离估计、经验证的绝对误差界、20米清除残差 | 米；误差界需覆盖全域极值及数值运算误差，任意固定小数不自带保证 |
| \(\operatorname{rd}_{\downarrow},\operatorname{rd}_{\uparrow}\) | 可靠向下/向上舍入 | 用于构造包含真值的区间；不能仅以常规浮点加减替代 |

参考预算（正文第9.3节）：默认B_ad=L_max=0；自适应试验B_ad=L_max=8；两者A_max=4096、J_max=16384，每请求最多重试2次。正常动作界严格小于A_max；改变自适应参数须重新核算。参数尚未通过Q4演练调优，不表示现实时间足够或接口会接受全部请求。

Q4符号作用域补充：目标圆域半径固定写1800米，R_c及候选r只表示接收半径；d_s(g)为源到检测点距离，r_g为引用Q3时的源径向距离。rho_t(q)为接收概率，清除半径直接写20米；varrho_t为Q3训练的PPO概率比，本稿不引入其更新公式。


## Q4 当前实现符号（2026-09-13，REVIEW）

以[当前正文](../paper/drafts/B-q4：全向与定向混合干扰源搜索定位与清除策略.md)为准。旧B_ad次数界、16384请求及38856秒界不作为默认参数或性能界。

| 符号 | 当前含义 | 单位与限制 |
|---|---|---|
| \(s_t,b_t,H_{c,t}\) | 位置、测向频道、有效历史 | 米、编号、事件；clear不改b_t |
| \(\mathcal P_t,\mathcal C_t,\mathcal E_t,\mathcal A_t,\mathcal U_t\) | 发现、清除、排除、活动、未知频道 | 发现16个只排除未知，活动仍须清除 |
| \(\widehat K_{c,t},R_Q\) | Q1正观测工程外包及最大距离评估 | 米；尚无完整浮点认证 |
| \(g_i,L_i,O_i,B_{ij},a_i,w_i\) | 粒子、半径下界、O/D相容性及工作权重 | 96粒子、72朝向，不是精确后验 |
| \(g^{\mathrm{est}},\bar g\) | 任务中心、信息粒子中心 | 米；前者可用256样本，后者96 Halton粒子 |
| \(p_i(q),\widehat\rho_c(q),\alpha_i(q)\) | 条件可见量、平均可见概率、半径近似因子 | 无量纲；只排序与筛选 |
| \(m_i^\pm,k_{ij},\mu_i^+,v_i^+,\mu^-\) | 正阴质量、三角核、条件均值及方差 | 均值米、方差平方米，其余无量纲 |
| \(\mathcal D_c(q)\) | 混合残余位置尺度 | 米；正分支RMS、阴分支平均距离 |
| \(\mathcal S_{21},\mathcal S_{64},\mathcal N(x,h)\) | 双环候选、解析网格、整盒近距测站 | 21非固定实际访问数，h为米 |
| \(v_c,F(A,j)\) | 主动目标访问数、开放路DP成本 | 次、米；8次不限制全部辅助测量 |
| \(t_o,r,b_o,\tau_{\mathrm{req}}\) | 超时、重试数、退避、完整等待额度 | 秒、次、秒、秒；默认现实余额>35.6秒 |

默认动作4096、请求12300、CPU900秒、虚拟360000秒；调度超过128轮转网格后备。机会光学门槛60米，Q1工程就绪19.999米；108中心之前另有局部光学池。
