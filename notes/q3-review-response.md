# 第三问审查意见逐项回应

审查日期：2026-09-12。依据用户提供的《q3-审查.md》，核对[题面](../problems/B/reference/B题.md)、[通信接口说明](../problems/B/reference/附件2_通信接口说明.md)、[第三问主稿](../paper/drafts/B-q3：全向干扰源搜索定位与清除策略.md)与[训练配套](../paper/drafts/B-q3：PPO训练方案.md)。本记录区分审查提出的潜在风险、文稿已有约束和本轮新增修订，不把审查中的每项判断直接当成已证实错误。

本轮仅修改论文与过程记录。没有新增程序、图片，也没有开展算法仿真、模拟器演练、校准或强化学习训练。

## 1 已清除频道与单频道单源

**核实。** 题面和接口明确各源频道不同、每频道最多一个源。原第3.2节已规定不扫描已清除频道，因此本题不存在被该假设遗漏的第二个同频源。审查对状态语义混淆的担忧有必要显式防范。

**修订。** 主稿第2.2节新增式（9a）：清除集合是原本有源集合的子集，原本有源与原本无源集合不相交，全部频道由未清除、已清除、已排除和未知四类分割。计数按频道去重。第4.1节明确清除后封存原定位证据，不能用之后的无信号反推原源位置在接收圆盘外。

**结论。** 已清除频道无需重新排除；撤销单频道单源前提后，现有计数和停止证明不能直接沿用。

## 2 源数上限与错误反馈

**核实。** 16是题设的绝对上限。在已确认16个不同频道有源且证据有效时，其他频道无源的逻辑成立；它不意味着16个源均已清除。任意伪信号或频道错配不属于现有题设反馈模型。

**修订。** 第3.3节新增式（17a）及证据有效标志，要求检查请求关联、唯一频道计数、记录有效性、集合不变量和联合几何相容性。数量排除记录保存其依赖证据；发现冲突后暂停数量捷径与成功终止。允许关闭数量捷径，采用逐频道完整覆盖。

**边界。** 可实施的相容性检查无法证明所有潜在错误均可被识别。若要对伪信号或频道错配给出额外保证，须先定义故障机制与可信复核方式；不以重复观测或关闭16上限捷径冒充一般容错证明。

## 3 凸外包能否产生不可靠清除点

**核实。** 审查在此处的不等式方向有误。只要真实可行集包含于外包，覆盖整个外包必然覆盖真实可行集。较大外包会使条件更严格，不会因其较大而错误放行清除。

**修订。** 第5.1节新增式（32a），明确最远距离的单调性和允许清除中心集合的反向包含关系。第8.4节要求每个拟执行点重新通过完整集合与坐标误差认证，禁止路径下界或未完成增补的有限约束结果直接授权执行。

**真正风险。** 外包没有包含真实可行集、仅验证采样点、忽略圆弧距离极值或用松弛下界代替可行路线，才可能造成错误许可。

## 4 非凸孔洞与闭合凸包

**核实。** “20米圆盘覆盖凸包，但没有覆盖凸包内真实残余区域”在集合包含成立时不可能。任意闭圆盘都是凸集，因此对固定圆心，非凸有界集合与其闭合凸包的最远距离相同。

**修订。** 新增式（32b）及凸组合、连续性证明，说明指定圆心的最远距离和最小覆盖圆半径均保持。精确闭合凸包不会改变可靠清除中心集合；更松的递推外包可能使可行中心集合缩小。

**保留。** 非凸排除信息仍完整存放在联合可行集与位置投影中，用于面积、概率和后续观测更新；不能据覆盖结论将其全部丢弃。

## 5 世界模型动作与PPO在策略样本

**核实。** 原主稿第8.7—8.8节与训练配套第6.6节已经禁止将规划器选择的轨迹按PPO建议概率更新。审查描述的是实现时必须避免的风险，并非原稿已经允许的训练方式。

**修订。** 新增式（67h）定义独立训练的实际行为分布：自由选择由旧PPO采样，预定后备为单动作分布。截断目标只在实际采样的自由选择步计算。规划器轨迹及其优势目标与独立PPO数据分开；仅改存最终动作的PPO概率、或只删除被替换那一步，不能修复整段轨迹的行为分布不匹配。

**用途。** 规划器轨迹仍可用于示范和校准；后续PPO微调须使用更新后冻结策略重新取得的交互样本。规划器与PPO状态访问分布的差异仍须通过独立组合验证评估。

## 6 后备接管与虚假高回报

**核实。** 原稿已设置不折扣的完整任务负时间奖励，明确后备接管不终止回合、全部尾段耗时纳入回报。因此，正确实现的目标不会把后备清除工作免计。但GAE截断和价值近似可能低估尾段代价，这一学习风险应进一步说明。

**修订。** 新增式（67i）和训练配套式（T11），显式分解自由选择前缀与后备尾段成本。新增式（67j）：取得实际后备尾段回报后，替换接管前最后一步的价值残差，再沿前缀递推；禁止重复计入尾段或将未完成尾段赋零成本。补充控制模式、已用动作数及耗时特征，并要求报告后备耗时、占比、触发状态和价值误差。

**结论。** 完整计费消除了“免费转交”这一目标漏洞，但不保证PPO一定学得好。若后备确实更快，交接可以符合总时间目标，不能把所有依赖后备的策略都判为错误。

## 7 清除与检测交替时的切频计时

**核实。** 原式（5）—（6）已保留测向机频道，清除不切频。审查中的交替序列适合补为明确的解析核对例。

**修订。** 新增式（6a）—（6b）。初始频道1，检测1、成功清除2、再检测2的非移动耗时分别为5、5、6秒，测向机频道分别为1、1、2；中间失败只将清除耗时改为3秒。世界模型各分支分别维护该状态，不能将动作目标频道直接写入测向机频道。

## 8 七步逼近是否忽略累积测向误差

**核实。** 原式（48）通过二维余弦定理已计入每一步的示向误差，式（49）取误差界内最坏情形；并非沿真实轴线的一维无误差缩放。

**修订。** 新增式（51a）—（51b），在每个实际观测起点重新写出真实源向量、新示向单位向量和逐步距离上界，证明任意满足硬界的相关误差序列均可逐次归纳。历史横向偏离已包含在当前真实向量中，不再重复添加同一误差。

**执行边界。** 七步结论要求按题设到达所提交坐标；额外位置误差按第7.4节另行处理。新增式（58a）给出有界位置误差下的充分步数，并要求误差稳态上界小于20米且保持接收。实际起点也未知或误差无界时，不沿用这一保证。

## 待独立复核

- 频道证据不变量与数量排除的依赖链，尤其是清除后无信号的处理。
- 外包包含关系、指定清除点认证与路径上下界的区分。
- 每步重新测向的二维归纳及额外位置误差的适用前提。
- PPO行为分布、回报计时和后备边界的优势递推是否一致。

以上为文稿层面的修订与解析核对，不表示相应控制器和训练器已经实现或通过性能验证。

## 第二轮审查回应（2026-09-12）

依据用户提供的《q3-审查 (1).md》继续核对。该意见提及带“_2”后缀的副本；当前仓库主稿已有完整第5节、第6—11节及参考文献，故不按副本截断描述重复生成后半稿。

### 1 覆盖二次函数的顶点

采纳表达改进。第3.1节补充顶点为750倍根号3米，约1299.04米，位于区间内并对应极小值；开口向上的二次函数在该闭区间的最大值取于端点。原覆盖证明的结果保持不变。

### 2 开边界与正容差

采纳对数值边界处理的要求，但不采用将无信号条件改为距离至少比接收半径大一个正容差的建议。这是内缩，会误删仍满足题设的近边界状态。

主稿式（23a）明确内缩、精确严格集合和闭集松弛的包含关系；式（23b）用外向舍入的残差区间给出满足、违反、未确定三种判定。数值精度不足时保留边界单元，不能通过扩大排除范围来获得看似确定的结果。相同规则适用于方向响应的5米严格下界、失败清除的20米严格下界和固定半径消元式。后验似然不自动替换为闭集近似。

### 3 第一次检测的切频

原稿已计入该成本，本轮单列第一动作和训练核对条件：初始测向机频道1，第一次检测其他频道需6秒操作时间；此前即使进行清除，测向机频道也不会改变。

### 4 后备回报与训练稳定性

采纳稳定性控制的目标，不采用只裁剪后备尾段回报或按后备方差单独缩放。前者使超过阈值的时间不再计价，后者改变前缀与尾段的相对权重，均破坏现有完整总时间目标。

训练配套新增第5.5节和式（T12）：保持原始回报与价值目标，只对同一批实际自由选择步的优势作共同标准化，排除强制步、规划器轨迹和填充位置，并处理单样本及低方差。沿用梯度范数限制、KL检查和有限更新轮数。明确这些是数值稳定措施，不是已经验证的方差下降或性能保证；PPO概率比截断不等于奖励裁剪。

### 5 全零掩码与逼近后备

采纳。主稿式（67k）和训练配套第4节明确：后备独立于自由候选生成；覆盖搜索完成、未知频道为空、但仍有未清除目标时，使用已有示向或近距离证据生成逼近后备。自由候选全被屏蔽时，合法后备成为唯一有效动作，不进入全零softmax。

后备仍接受几何、坐标和预算硬检查，不能强行执行非法动作。规划中确无合法后继的非终止分支按不可行成本处理，不删除分支或赋零成本；数值评分未完成与数学不可行分开处理。

### 6 Jung界及第5节衔接

不采纳审查给出的通用下界。正确式为

\[
\frac{D(K)}2\le r_*(K)\le\frac{D(K)}{\sqrt3}.
\]

长度为2的线段有最小覆盖圆半径1，小于2除以根号3，已否定审查建议的下界；等边三角形达到正确上界。D除以根号2虽是更松的上界，也不能修复那个错误下界。

主稿保留式（30），补最小覆盖圆支撑点证明，并说明未通过圆覆盖认证时继续主动定位或调用第7节后备。现有章节没有截断，无须重复补写。

### 7 排版与示意图

审查对标准表格和示意图的建议是后续排版建议。按用户持续有效的“不加图、不加代码、不做仿真”要求，本轮不新增图或实验素材，继续以正文公式、条件表达式及已有训练对照表说明算法。对照策略位于训练配套第9.1节，主稿第9.1节是正常决策流程，引用位置予以区分。

第二轮仍保持待队友独立复核；正文与训练配套均未声称已经实现或验证世界模型及PPO。

## 第三轮审查及职责划分回应（2026-09-12）

本轮依据《q3-审查 (2).md》和用户补充的职责要求修改。Q1确定性定位及clear-ready接口已有实现；按用户说明，Q3-v1目前连接discovery与Q1。此次不修改Q1或Q3程序，文稿重点转为评估后的补测选点、多频道调度和清除顺序，不把评估适配等同于完整策略已实现。

### 1 Q1复用与Q3重点

主稿第1.1节用式（I1）—（I2）描述调用契约，区分Q1的CLEAR_READY、COVERAGE_UNCERTAIN与MODEL_CONFLICT。Q3需要补测的任务状态不能吞并冲突；第4—5节定位为接口约束说明，压缩重复证明和自行求解的描述。

第6.4节给出明确的横向/纵向候选、预测响应后再次调用Q1，以及补测和清除的统一动作比较。任务全就绪时使用固定认证点开放路线作为终端成本；否则可以有限展开参考后备，预算不足退回保守上界。该方法旨在让选点反映后续任务变化，仍须评价计算开销与估价精度。

当前清除排序优先使用Q1返回的指定认证点。只有接口允许新位置复核或提供可靠中心集合时才启用邻域扩展，不能在Q3复制一套几何认证。Q1未支持的观测类型须保留原始记录并明确适配能力，不能伪造输入或宣称已融合全部信息。

### 2 先清除后检测的切频

采纳补充条件例。主稿第2.1节与训练配套第6.1节同时说明：初始在原点先清除频道2，测向机仍为频道1；之后到q检测频道2，仍包含1秒切频。成功清除时累计耗时为5加移动时间再加6秒，失败时将最初5秒改为3秒。

这是协议条件计时，不代表策略会对初始未知频道盲目清除。实际测向机频道与动作目标频道始终分开。

### 3 GRU状态恢复

原方案已要求当前参数下恢复历史，本轮用式（T13）明确索引：从回合开始前向到片段起点，以当前参数计算隐藏状态，再停止前缀梯度、对后64步作截断反向传播。旧概率保持采样时值；新概率不能使用旧隐藏状态代替当前状态。

16步预热加48步损失只能作为替代近似。若预热从旧隐藏状态开始，有限预热并不保证消除历史不匹配。前缀重算保证前向状态的口径，但截断梯度仍不同于完整回合反向传播。内部预测分支不进入真实循环历史。

### 4 圆弧与外包表示

采纳接口表示要求。Q1返回的外包若是圆弧复合边界，需要覆盖弧段内部极值；若是可靠外切多边形，则全部顶点的距离足以覆盖该多边形。弧上采样点连成的内接多边形不具有外包保证。

本轮只明确Q1输出契约和保证等级，不在Q3新增圆弧求极值或外包算法。工程保护余量不自动升级为严格浮点或区间认证。

### 5 动态候选快照

采纳。训练配套第3.3节规定保存候选输入特征矩阵、有效行数、掩码、采样行号及物理动作、旧对数概率和版本。更新直接读取本批快照，不调用候选生成器或路径排序器。可训练编码器的旧输出不能代替原始输入，新参数下仍要重新编码。

缓存是当前在策略采样批次的轨迹缓冲，不是可跨旧策略任意重放的经验池。行号、掩码与实际执行动作不一致时拒绝该批更新，不通过重新排序来猜测修复。

### 6 两阶段路径热启动

有条件采纳。具备邻域接口时，可用认证松弛距离矩阵先求开放TSP顺序，再固定顺序优化位置、调用Q1认证，最后在预算内改进顺序或分支定界。

不采纳“所得顺序已经接近最优、因此一定大幅加速”的未经验证推断，也不把8个目标设为普适性能阈值。当前固定点接口直接使用开放式子集动态规划，不为套用邻域方法重复开发几何。

### 7 优势标准化的空样本保护

原式（T12）已覆盖0/1个自由选择样本，本轮再明确先判断数量后计算统计量。0个时跳过策略项且不取空均值；1个时保留原优势；至少2个且方差足够时才标准化。非有限目标触发批次核查，不通过填零掩盖异常。

### 8 概率异常与误差界扩张

采纳对数值异常设置保护的目标，不采用临时扩大测角误差硬界。主稿式（W10）给出对数域权重归一化；式（W11）区分低概率但有效、近似失配或下溢、真实硬约束冲突。诊断采用概率质量或量化读数区间概率，不对连续密度套用固定概率阈值。

重算积分或重建场景近似必须保留原1度误差界、固定半径和完整历史；有限样本全部失配不等于硬可行集为空。概率模型不可用而Q1状态正常时仍可确定性选点和调度，真实冲突则核查。改变误差硬界必须作为独立模型重新分析，不能用来挽救当前清除证书、七步或时间保证。

本轮无代码、图和算法实验。文稿提出的训练稳定措施、两阶段路径方法和状态相关终端估价均未宣称已取得性能收益。

## 第四轮审查回应（2026-09-12）

本轮依据《q3-审查 (3).md》继续同步总览与主稿。审查中的 `modeling_7.md`、带版本后缀的草稿和训练方案对应仓库现有文档，本轮直接修订原文件，不另建导出副本。意见涉及的接口边界、数值判定、方向覆盖状态和训练数据隔离均采纳；这些补充是文稿契约，不表示新增了求解器、训练或仿真结果。

### 1 Q1/Q3职责及状态映射

[建模总览](modeling.md)第7.2—7.3节与主稿第1.1节保持一致：Q1负责确定性定位与指定清除点评估；Q3负责在NEEDS_MORE_MEASUREMENT时生成下一检测点，并决定测量频道、清除顺序和预算。CLEAR_READY映射为可清除任务，COVERAGE_UNCERTAIN映射为待进一步评估或补测，MODEL_CONFLICT保留独立核查分支。Q1不承担动态选点职责，Q3也不重建几何定位或20米判定。

总览原先“自行按最小圆半径清除”和“邻域TSP为默认主线”的文字同步改正。当前使用Q1已返回的固定认证点；邻域扩展仍以Q1支持另一个指定点认证或中心集合为前提。保守外包或数值未确定不能被解释为已经证明必须增加实测；可先请求细化评估。

### 2 浮点残差与无信号边界

总览第3.2节补全严格与非严格不等式的三值判定，并在第7.3—7.4节分别落实到指定点覆盖与无信号约束。认证残差区间须覆盖真值；用于空间单元时还须覆盖整个单元。严格约束在下界大于零时满足、上界不大于零时违反，其余未确定；非严格约束相应改为下界不小于零、上界小于零。

无信号精确事件仍是d大于固定接收半径r；闭集外松弛可暂留边界，正容差内缩可能删除真实状态。未确定单元保留或细分，内缩近似为空不证明无源。完整覆盖排除依赖逐频道真实有效测量，已发现未清除频道的矛盾记录进入冲突核查，清除频道仍保留成功回执。数值容差不改变题设±1度或物理距离阈值，Q1现有工程余量不被包装为已实现的严格区间认证。

### 3 Q4方向覆盖的结论等级

总览没有单独的论文摘要，因此在开篇概述、第1节路线状态及第2节整体路线前置标注：Q4方向覆盖的充分条件和混合源搜索完备性尚待证明，详见第8.3节。此次仅同步结论状态，不扩展为Q4建模任务，也不把Q3全向源保证外推至定向源。

### 4 PPO当前批次与数据隔离

总览第7.5.1节明确在策略轨迹缓存不作为离线经验池：只保存本轮冻结行为策略生成的当前采样批次，可按PPO约定作有限轮更新，随后重新采样。候选输入、掩码、实际动作及旧概率使用采样时快照；规划器执行和世界模型内部推演均不冒充PPO策略梯度数据。强制后备完整计入回报，但其动作不进入自由选择步策略项。与训练方案第3.3、6.4—6.6节口径一致，本轮无需重复修改已写明这些约束的训练配套。

### 5 基准虚拟时间的一致性

总览第7.5节与主稿第10.2—10.3节统一引用式（71）—（73），对应M不超过236、C不超过16、F为0、B不超过M、L小于57032米，得到T严格小于12902.4秒。两处集中列明全向静态源、10—16个源、1000—1500米固定接收半径、±1度示向误差、精确动作坐标、正常有效响应及可靠数值上界，并限定从原点和初始频道1开始的七点扫描、发现后立即保证逼近清除的基准。

该数值不是实测耗时，也不适用于任意世界模型/PPO优化前缀。优化后的总时间需另计前缀与当前状态的后备完成上界，额外位置误差则重新计算步数和路程。任务继续保持REVIEW，待队友独立复核。

## 第五轮审查回应（2026-09-12）

本轮依据《q3-审查 (4).md》核实五类事项。继续遵守用户的论文文字、公式及条件讨论范围，不把审查中的程序开发、图片和蒙特卡洛建议视为启动这些工作的授权。Q1已有接口继续复用；其已实现状态与尚待扩大验证的范围分开记录。

### 1 长后备尾段、优势裁剪与价值更新

采纳风险分析，不直接采用默认的−10至10或分位数优势裁剪。训练配套第5.6节说明：完整尾段回报向接管前第t步优势传播的系数为λ的(t_f−1−t)次方，最后一步不衰减，方差还涉及其他残差与协方差；10000秒在固定时间尺度下对应−100，本身不等于数值溢出。

[PPO原文](https://arxiv.org/pdf/1707.06347)第3节截断的是新旧动作概率比。配套式（T14）直接比较在相同批次、旧策略参数处，另行裁剪优势带来的梯度差，表明其一般改变样本权重。这与裁剪时间奖励不同：可以保留回报账本，但策略代理目标仍发生变化，不能声称梯度等价。若今后试验此变体，须单列并报告截断位置、比例、完整时间及失败率；本轮没有试验或预设其收益。

补式（T15）及式（T9）的价值样本索引，价值目标始终用原始优势/完整尾段生成，不能用标准化或裁剪优势重建。默认保留同批优势标准化、有限梯度范数和KL保护；出现非有限值时停止更新并查因。每次试更新后检查联合参数及策略KL，超限撤回本次参数和优化器状态，再结束该批联合更新，避免价值头经共享编码器继续改变策略。合法的高代价样本保留，不以离群理由删去。以上均为待验证的训练设计，不是稳定性定理。

### 2 七点无信号证明的实际回执前提

原文已要求每个频道持有自己的有效记录，本轮在主稿第3.2节式（16a）显式补齐“频道—骨干点”的缺失、无信号、有信号标志。排除一个未知频道需要七项均为有效无信号，且回执匹配本任务的请求、频道与实际坐标，满足HTTP状态、accepted及字段检查。重试按同一请求标识去重；未发送、拒绝、超时未核实、中断或提前退出均保留缺失，不填成无信号。附近位置只有在实际覆盖得到认证时才作为证据。

总览第7.4节与训练配套第2.4节同步。七点是充分证据之一，不能因这项补充而强迫已取得其他可靠覆盖证明或16源数量证明的频道再扫七点。缺失记录本身也不构成几何冲突；没有其他证明时继续保持未知，预算不足则如实报告未完成。

### 3 Q4“极大张角”的歧义

有条件采纳作为未来方向覆盖的研究入口，不照抄“极大张角严格大于180度”。总览第8.3节定义最大相邻方向空隙，并给出固定源位置、有限非零方向集的局部等价条件：每个闭180度辐射角域至少命中一个方向，当且仅当最大空隙不超过180度。两个对踵方向说明等号可行；0度和60度的方向集则存在300度空隙，不能保证覆盖。

若审查的张角指最短包络弧长度，大于180度可作充分条件但不是必要条件。局部几何澄清不等于已完成Q4全域布局：仍需对整个目标圆域认证距离与方向、处理源与检测点重合的方位语义，并保证逐频道记录完整。这些继续标为待办。

### 4 跨问符号与单位

[符号索引](symbols.md)补齐共同物理量及Q3、PPO的含义、单位、作用域和正文位置。重点区分Q1的圆域半径R、Q2/Q3的接收半径R/R_j、清除半径ρ、PPO概率比ϱ，以及源位置集、允许检测/清除中心集和频道集合；时间为秒，价值网络输出为固定尺度下的回报，不能直接混用。不同观测融合程度下的P、K和真实可行集不强行视为相等。本轮不修改已有Q1接口字段或其他问的程序。

### 5 待办优先级与范围

在TODO中保留团队后续确定Q3/Q4主程序owner、验收复用Q1的可运行基线、再安排联调与针对性数值验证的顺序；本任务不代替团队指定人员，也不把Q1已完成接口重写成尚未开发。文稿及训练示范中残留的无条件邻域TSP描述同步为固定认证点默认、邻域能力可选。仍保持REVIEW，未新增程序、图、训练、校准、仿真或模拟器记录。

## 第六轮审查裁决（2026-09-12）

本轮依据《q3-审查 (5).md》，按用户要求直接列明不应修改的结论。审查是否反复不能证明审查人的动机；可以核实的是，本轮若干“遗漏”已在前文说明，另有部分建议需要限制解释。处理原则是核对实际文字、前提和反例，不以审查次数为理由接受或拒绝意见，也不为每次重复提醒重写已经成立的模型。

| 意见 | 裁决 | 依据与本轮处理 |
|---|---|---|
| 连续清除不同频道不切测向频道 | 既有公式已覆盖，可补条件例 | 式（6）对任意清除均保持频道；补两次清除后检测的16秒非移动计时例，不修改时间模型 |
| 中途发现信号即退出未知扫描 | 已覆盖，仅补就地说明 | 第3.2节已有只扫描未知频道、已发现频道转交定位的规则，式（16a）也以未知频道为前提；补显式转入活动集合 |
| 未确定单元不能冒充严格满足点 | 采纳这个限定含义 | 保留单元及其未确定标志，不用代表点替代整个单元；不接受扩大为“外包中有未确定单元就禁止清除” |
| 后备必须改用当前位置的最远距离重启 | 不采纳为默认替换；修正相关指代 | 旧方向属于旧测向点，当前位置距离上界不能补出新方向；默认仍用历史参考点生成后备目标，补中心一致性与真实转场计费 |
| 快照保存未经编码的物理输入 | 第三轮已明确，本轮仅加强措辞 | 原文已禁止用可训练编码器输出代替输入，并要求当前参数重编码；补坐标、距离、时间等例子，不新增一套缓存机制 |
| 熵系数需配合优势标准化 | 有条件采纳 | 补梯度、候选数与熵的诊断；不机械随100秒尺度换算，不把探索不足单因归咎于熵衰减；梯度差中相同熵项已抵消 |
| 用旧隐藏状态加16—32步预热节约计算 | 原有近似选项的细化 | 补复杂度前提、起止索引及窗口口径；默认保持精确前向恢复，不把有限预热说成精确恢复 |

### 1 不能删除保守外包的清除保证

“是否满足某条严格观测约束”与“整个可能源区域是否在清除圆内”是不同问题。未确定单元不能升级为严格可行点，也不能被随意丢弃；但可靠外包覆盖所有可能源位置，只会使指定点清除判定更保守。主稿第4.1.1节补充

\[
G_j\in\mathcal S_{j,t}\subseteq K,\qquad
\overline R_K(c)+\eta_c\le20
\quad\Longrightarrow\quad
\|G_j-\widetilde c\|\le20.
\]

其中输出误差仍满足既有界。无需先证明K中的每一点真实可行。因此不修改第5节的外包全域认证规则；禁止的是拿某个未确定代表点代替完整覆盖认证。若清除覆盖本身无法认证，则仍不授权清除。这与审查的狭义提醒一致，与“一律不通过清除认证”的扩大解释不同。

### 2 不能只重算当前位置距离、保留旧方向

原第7.2节已明确允许机器狗离开测向参考点s后，直接执行由s生成的后备目标q，实际移动距离取当前位置到q。单步收缩的几何推导可对一般有限距离上界成立，1500米条件用于本题的首次再接收和七步数值保证；它不是“机器狗无论走到哪里，当前位置都距源1500米以内”的断言。

反例可直接说明错误替换：设旧测向点s=(0,0)、源G=(1000,0)、旧示向正东；机器狗转场至p=(1000,2000)，此时实际距离上界可取2000。若把旧方向搬到p并前进κ乘2000，目标到源距离为2000乘根号(1+κ²)，不仅不小于κ乘2000，反而超过原距离。重算距离没有提供当前位置的示向，原收缩前提已经丢失。

默认做法保持q=s+κUu，且U与s绑定、不超过1500；实际转场按距离(p,q)计，第一目标仍满足保证再接收。此次确实修正两处可能误用：第6.4节明确Q1上界必须绑定相同参考中心，若Q1半径属于另一个圆心，则加两中心距离后再与1500取小；式（53）首分支改为在参考点s清除，不再含糊写“当前位置”。恢复后备时另计转场及首次检测可能切频，补335.2加距离(p,s)/5加1秒的保守单源界，完整任务仍用当前状态后备预算。

### 3 已写明的快照与GRU边界不反复翻案

训练第3.3节原文已经规定保存固定预处理后的输入、禁止保存编码器输出代替原始输入、当前参数重新计算编码。本轮只把“未编码的物理输入”及注意力层说明得更直接；固定单位换算或正余弦变换可以保留，并不意味着必须保存完全未处理的原始日志。

训练第6.5节原文也已有旧状态加有限预热的近似选项。独立恢复各片段前缀时，单轮前向开销可随回合长度呈二次增长；同参数版本内顺序复用可降低重复计算，但更新参数后不能继续把旧缓存当精确状态。本轮列出16/32步预热与64步损失的预算区别，明确起点取旧状态时仍有偏差。没有收缩性或其他误差界，不能声称有限预热消除偏差；默认方案不改成近似方案。

### 4 熵项调节不能改写已有梯度差

式（T14）比较相同批次、相同网络位置下，仅替换优势权重的两种策略代理梯度；保持同一熵正则与价值项时，相同项在作差中抵消，并不存在“漏加熵梯度”需要修公式的问题。

参数调节则是另一回事。第7节增加标准化后优势、合法候选数、归一化熵及梯度量级的诊断。熵系数0.01仍是待验证起点；单一合法候选时熵为零，调大系数也不能产生新动作。探索不足先核对候选、价值误差、学习率与KL，再考虑初值或衰减速度，不能仅凭原始时间尺度或探索现象机械修改。

### 5 本轮停止改动的边界

保留正确的切频公式、七点排除与发现分流、严格边界三值规则、外包全域认证、参考点收缩证明、原始输入快照及精确/近似GRU的区别。今后若再次提出这些意见，应指出新的反例、前提变化或具体不一致位置；没有新增依据时只引用现有说明，不再重复扩写。已发现的中心绑定和位置指代问题已实际修正，不能因为审查反复而回避自身问题。

本轮仍仅修改文稿及协作记录，不新增程序、图、训练或仿真，任务保持REVIEW。
