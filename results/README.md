# 程序生成结果

- `figures/`：程序直接生成的 PNG、PDF、SVG 等图片。
- `tables/`：CSV、Markdown、LaTeX 表格及指标结果。
- `excel/`：需要提交、检查或用于论文的 XLSX 文件。

建议文件名包含问题号和含义，例如 `q1_distribution.png`、`q2_metrics.csv`。不要手工修改生成结果；修改代码或输入后重新运行。

每个最终数字应能追溯到：输入数据、代码、参数、输出文件和论文位置。
